import React from "react";
import styled from "styled-components";

function QuestionForm(props) {
    return <Container>day la QuestionForm</Container>;
}

const Container = styled.div``;

export default QuestionForm;
